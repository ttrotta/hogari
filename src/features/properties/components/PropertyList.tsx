import { PropertyCard } from "./PropertyCard";

export function PropertyList() {
  return (
    <div className="flex flex-col gap-4">
      {/* TODO: Map over properties and render PropertyCard */}
      <PropertyCard />
    </div>
  );
}
